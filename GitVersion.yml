mode: ContinuousDeployment
next-version: 0.1.0
branches:
  main:
    regex: ^master$|^main$
    mode: ContinuousDeployment
    tag: ci
    increment: Patch
  feature:
    regex: ^features?[/-]
    mode: ContinuousDeployment
    tag: alpha
    increment: Minor
  fix:
    regex: ^(bug)?fix(es)?[/-]
    mode: ContinuousDeployment
    tag: beta
    increment: Patch
  pull-request:
    regex: ^(pull|pull\-requests|pr)[/-]
    mode: ContinuousDeployment
    tag: pr
    increment: Inherit
